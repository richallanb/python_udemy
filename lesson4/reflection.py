from count import *

## Returns the type of the variable (module)
print(type(count))

## Returns a listing of all functions/attributes
print(dir(count))

## Returns type function
print(type(show_count))

## Shows attributes on the function
print(dir(show_count))

## Prints the function name attribute
print(show_count.__name__)